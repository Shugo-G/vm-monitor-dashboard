# Django configuration
DJANGO_SECRET_KEY=your-secret-key-here
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=server-monitor.ushuaia.gob.ar,localhost,127.0.0.1
TZ=America/Argentina/Ushuaia

# Database path (relative to container or absolute inside mount)
DB_PATH=/app/data/db.sqlite3

# Registry configuration (optional, for pre-built images)
# DOCKER_REGISTRY=ghcr.io/yourusername
# APP_VERSION=latest
